// 题面依据用户提供的 Markdown，并逐页对照下列原卷图片校正。
// 答案与解析为独立推导、程序验算的学习参考，不冒充 CCF 官方答案。
const sourceUrl = 'https://mp.weixin.qq.com/s/M3yYCsnZqgK0MTEXiavb-A';
const optionsOf = values => Object.fromEntries(values.map((value, index) => [String.fromCharCode(65 + index), value]));
function choice(number, question, options, answer, tags, explanation) {
  return { id: `2026-choice-${number}`, number, question, options: optionsOf(options), answer, tags, explanation, sourceUrl };
}
function part(originalNumber, text, options, answer, score, explanation) {
  return { originalNumber, text, options: optionsOf(options), answers: [answer], multiple: false, score, explanation };
}
function program(type, number, statement, tags, questions) {
  const id = `2026-${type}-${number}`;
  // 与现有题库一致，将原卷行号保留在代码文本中（空行也计数）。
  statement = statement.replace(/```cpp\n([\s\S]*?)\n```/g, (_, code) =>
    '```cpp\n' + code.split('\n').map((line, index) => `${String(index + 1).padStart(2, '0')} ${line}`).join('\n') + '\n```');
  return {
    id, year: '2026', type, number,
    title: `${type === 'reading' ? '阅读程序' : '完善程序'}第 ${number} 题`,
    description: statement, statement, tags, sourceUrl,
    questions: questions.map((item, index) => ({ ...item, id: `${id}-${index + 1}`, number: index + 1 })),
  };
}

export const csp2026ChoicePapers = {
  '2026': [
    choice(1, '下列 C++ 数据类型中，能够精确存储 `10^18 + 1` 这个整数的是（ ）。',
      ['`float`', '`long long`', '`double`', '`int`'], 'B', ['数据类型与运算'],
      '参考答案为 B。\n\n**详细解析：** 常用竞赛环境中，`long long` 是 64 位有符号整数，最大值为 `2^63-1≈9.22×10^18`，可以精确表示 `10^18+1`。32 位 `int` 的范围不足；`float`、`double` 虽然能表示数量级为 `10^18` 的数，但有效精度不足以区分该处相邻的每个整数。注意：能表示这么大的数，不等于能精确存储这个数。'),
    choice(2, '十六进制数 `2F5` 转换为八进制数是（ ）。', ['1364', '1635', '1405', '1365'], 'D', ['进制转换'],
      '参考答案为 D（1365）。\n\n**详细解析：** 先转成二进制：`2F5₁₆ = 0010 1111 0101₂`。从右向左每三位一组，得到 `001 011 110 101`，分别对应八进制数字 `1、3、6、5`。也可验算：`2×256+15×16+5=757`，而 `1×512+3×64+6×8+5=757`。'),
    choice(3, '执行下列 C++ 代码，输出是（ ）。\n\n```cpp\nint a = 7, b = 3;\nstd::cout << a / b * b + a % b;\n```',
      ['9', '10', '7', '6'], 'C', ['数据类型与运算'],
      '参考答案为 C（7）。\n\n**详细解析：** `a` 和 `b` 都是整数，所以 `7/3=2`、`7%3=1`。乘除取模优先于加法，除法和乘法按从左到右计算，结果为 `2×3+1=7`。这也正是整除关系：被除数 = 商×除数 + 余数。'),
    choice(4, '初始时栈为空，将 1、2、3、4 依次入栈，入栈过程中允许随时出栈。下列出栈序列中不可能出现的是（ ）。',
      ['2，4，3，1', '1，2，3，4', '3，1，2，4', '1，4，3，2'], 'C', ['栈与队列'],
      '参考答案为 C。\n\n**详细解析：** 若第一个弹出 3，必须先依次压入 `1、2、3`。弹出 3 后栈顶是 2，1 被压在 2 下面，因此不能接着弹出 1；即使先压入 4，也不会改变 2 在 1 上方这一事实。其余序列都能实现：A 先压入 1、2 并弹出 2，再压入 3、4，依次弹出 4、3、1；B 每入一个立即弹出；D 弹出 1 后压入 2、3、4，再依次弹出。'),
    choice(5, '一棵有 100 个结点的完全二叉树，其叶子结点个数是（ ）。', ['49', '50', '64', '51'], 'B', ['树与二叉树'],
      '参考答案为 B（50）。\n\n**详细解析：** 完全二叉树按层从 1 编号，编号为 `i` 的结点若有左孩子，其编号就是 `2i`。因此 `i≤50` 的结点有孩子，`51..100` 都没有孩子，是叶子。叶子数为 `100-⌊100/2⌋=50`。最后一层不必满，不能直接套满二叉树的层数公式。'),
    choice(6, '执行下列代码后 `s` 的值是（ ）。\n\n```cpp\nint s = 0;\nfor (int i = 1; i <= 100; i++)\n    if (i % 3 == 0 || i % 5 == 0)\n        s += i;\n```',
      ['3048', '2733', '2318', '2418'], 'D', ['容斥原理', '分支与循环'],
      '参考答案为 D（2418）。\n\n**详细解析：** 程序累加 1 到 100 中能被 3 或 5 整除的数。3 的倍数之和是 `3×(1+…+33)=1683`，5 的倍数之和是 `5×(1+…+20)=1050`。15 的倍数在两组中重复，需减去 `15×(1+…+6)=315`。所以 `s=1683+1050-315=2418`。逻辑或只让每个满足条件的数被累加一次。'),
    choice(7, '上楼梯每步可上 1 级、2 级或 3 级，从地面（可视为第 0 级）走到第 8 级台阶共有多少种不同走法（ ）。',
      ['44', '121', '149', '81'], 'D', ['递推', '动态规划'],
      '参考答案为 D（81）。\n\n**详细解析：** 设到第 `i` 级有 `f[i]` 种走法。按最后一步上 1、2、3 级分类，`f[i]=f[i-1]+f[i-2]+f[i-3]`，三类不重不漏。取 `f[0]=1`，负下标为 0，逐步得到 `f[1..8]=1,2,4,7,13,24,44,81`。本题要区分步子的顺序，例如 `1+3` 与 `3+1` 是不同走法。'),
    choice(8, '下图为 `5×5` 网格，行号、列号均从 0 开始，`#` 为障碍，`.` 为可通行格：\n\n```text\nS..#.\n...#.\n...#.\n##..E\n...#.\n```\n\n从 S 出发做广度优先搜索（BFS）：初始时把 S 入队；每次取出队首格子，按“上、下、左、右”（上 = 行号减 1，下 = 行号加 1，左 = 列号减 1，右 = 列号加 1）的顺序遍历它的四个相邻格子，越界、障碍或已访问的格子跳过，其余格子标记为已访问并入队。当 E 第一次入队时，已经入队过的格子（含 S 和 E）共有多少个（ ）。',
      ['15', '12', '14', '13'], 'C', ['图论', '广度优先搜索'],
      '参考答案为 C（14）。\n\n**详细解析：** 必须按指定方向顺序模拟队列，不能只数最短路径上的格子。入队顺序（行，列）为：\n\n```text\n1:(0,0)  2:(1,0)  3:(0,1)\n4:(2,0)  5:(1,1)  6:(0,2)\n7:(2,1)  8:(1,2)  9:(2,2)\n10:(3,2)  11:(4,2)  12:(3,3)\n13:(4,1)  14:(3,4)=E\n```\n\n处理 `(3,2)` 时先把下面的 `(4,2)` 入队，再把右边的 `(3,3)` 入队。因此处理 `(3,3)`、发现 E 之前，还会先处理 `(4,2)` 并让 `(4,1)` 入队。E 是第 14 个入队的格子。'),
    choice(9, '满足 `1≤n≤100` 且 `gcd(n,60)=6` 的正整数 `n` 共有多少个（ ）。', ['8', '6', '4', '5'], 'B', ['数学与数论', '最大公约数'],
      '参考答案为 B（6）。\n\n**详细解析：** `n` 必须是 6 的倍数，令 `n=6k`，则 `1≤k≤16`，且 `gcd(6k,60)=6×gcd(k,10)`。所以要求 `gcd(k,10)=1`，即 k 既不是 2 的倍数，也不是 5 的倍数。可取 `k=1,3,7,9,11,13`，对应 `n=6,18,42,54,66,78`，共 6 个。'),
    choice(10, '某国硬币面值为 1 元、4 元、6 元且数量不限，凑出 9 元最少需要多少枚（ ）。', ['3', '4', '5', '2'], 'A', ['枚举', '动态规划'],
      '参考答案为 A（3）。\n\n**详细解析：** `4+4+1=9`，所以 3 枚可以凑出。1 枚显然不行；2 枚硬币的所有可能和为 `2、5、7、8、10、12`，没有 9，因此不可能更少。注意每次先选最大面值的贪心会得到 `6+1+1+1` 共 4 枚，本题这种贪心不最优。'),
    choice(11, '执行下列代码，输出是（ ）。\n\n```cpp\nint a[5] = {1, 3, 5, 7, 9};\nint *p = a + 2;\n*(p - 1) = p[0] + p[2];\np[1] = *(a + 1) - a[0];\ncout << a[1] << "," << a[3];\n```',
      ['`14,13`', '`8,13`', '`14,7`', '`14,2`'], 'A', ['指针与引用', '数组'],
      '参考答案为 A（14,13）。\n\n**详细解析：** `p` 指向 `a[2]`，所以 `*(p-1)` 是 `a[1]`，`p[0]` 是 `a[2]`，`p[2]` 是 `a[4]`。第一次赋值把 `a[1]` 改成 `5+9=14`。随后 `p[1]` 即 `a[3]`，右侧使用的是已经更新的 `a[1]`，得到 `14-1=13`。最终输出 `14,13`。'),
    choice(12, '在含 1000 个互不相同元素的升序数组中，用二分法查找给定值（返回元素位置或报告不存在），最坏情况下需要与数组元素比较多少次（ ）。',
      ['500', '9', '11', '10'], 'D', ['排序与查找', '二分查找'],
      '参考答案为 D（10）。\n\n**详细解析：** 每比较一次，剩余待查区间至多缩小约一半。最坏区间长度依次为 `1000→500→250→125→62→31→15→7→3→1→0`，最多比较 10 个中间元素。也可用 `⌊log₂1000⌋+1=10`；9 层二分查找最多容纳 `2^9-1=511` 个元素，不足以覆盖 1000 个。'),
    choice(13, '数组 `a[1..n]` 的前缀和数组 `s`（即 `s[i]=a[1]+a[2]+…+a[i]`）满足 `s[i]=3i²+i`，则 `a[10]` 的值是（ ）。',
      ['252', '310', '58', '61'], 'C', ['前缀和'],
      '参考答案为 C（58）。\n\n**详细解析：** 相邻两个前缀和作差，之前各项相消，剩下当前项：`a[10]=s[10]-s[9]`。其中 `s[10]=3×100+10=310`，`s[9]=3×81+9=252`，所以 `a[10]=310-252=58`。310 是前十项总和，不是第十项。'),
    choice(14, '数轴上有 7 个点，坐标分别为 `1、3、4、7、10、15、20`。在数轴上选取一个整数坐标点 P，使 P 到这 7 个点的距离之和最小，这个最小距离和是（ ）。',
      ['37', '42', '40', '38'], 'A', ['数学与数论', '中位数'],
      '参考答案为 A（37）。\n\n**详细解析：** 将两端配对：到 1 和 20 的距离之和至少为 19，到 3 和 15 的距离之和至少为 12，到 4 和 10 的距离之和至少为 6；到剩下的点 7 的距离至少为 0。所以总和至少 `19+12+6=37`。取中位数 `P=7` 时四个下界同时达到，实际距离和为 `6+4+3+0+3+8+13=37`。'),
    choice(15, '一个无向图有 10 个顶点，其中 4 个顶点的度为 3，其余顶点的度均为 4，则该图的边数是（ ）。',
      ['36', '18', '17', '20'], 'B', ['图论', '度数'],
      '参考答案为 B（18）。\n\n**详细解析：** 总度数为 `4×3+(10-4)×4=36`。每条无向边有两个端点，会在总度数中被统计两次，因此由握手定理可得边数 `36÷2=18`。36 是度数总和，不能直接当作边数。'),
  ],
};

export const csp2026ProgramProblems = [
  program('reading', 1, '```cpp\n#include <iostream>\nusing namespace std;\nint main() {\n    int n;\n    cin >> n;\n    int x = 1, y = 1;\n    while (n > 0) {\n        if (n % 2 == 0) {\n            ++x;\n        } else {\n            ++x;\n            ++y;\n        }\n        n = n / 2;\n    }\n    cout << x << \' \' << y << endl;\n    return 0;\n}\n```\n\n以下问题均假定输入的 `n` 为不超过 `2^31-1` 的非负整数。',
    ['二进制', '分支与循环'], [
      part(16, '（1 分）当输入为 `3` 时，程序输出为 `3 3`。（ ）', ['正确', '错误'], 'A', 1,
        '参考答案为 A（正确）。\n\n**详细解析：** 初始 `x=y=1`。第一次 `n=3` 为奇数，执行第 11、12 行，得到 `x=y=2`，然后 n 变成 1；第二次仍为奇数，得到 `x=y=3`，n 变成 0 并退出。所以输出 `3 3`。'),
      part(17, '将第 11 行的 `++x;` 删除后，程序输出的两个数一定相等。（ ）', ['正确', '错误'], 'B', 1.5,
        '参考答案为 B（错误）。\n\n**详细解析：** 删除后，遇到偶数只增加 x，遇到奇数只增加 y，两者没有相等的保证。例如输入 `1`，进入奇数分支后 x 仍为 1，y 变为 2，随后 n 变为 0，输出 `1 2`，已构成反例。'),
      part(18, '假设输入为非负整数，则程序输出的第一个数一定不小于第二个数。（ ）', ['正确', '错误'], 'A', 1.5,
        '参考答案为 A（正确）。\n\n**详细解析：** 初始 `x=y=1`。每轮 x 都增加 1，而 y 只在 n 为奇数时增加 1，所以差值 `x-y` 只会不变或增加，不会变负。换成二进制理解：x 是“二进制位数 +1”，y 是“二进制中 1 的个数 +1”，1 的个数不会超过总位数。输入 0 时不循环，仍有 x=y=1。'),
      part(19, '将第 7 行的 `while (n > 0)` 改为 `while (n >= 0)` 后，程序可能出现的问题是（ ）。',
        ['陷入死循环', '输出结果比原来大', '输出结果比原来小', '输出结果不受影响'], 'A', 3,
        '参考答案为 A（陷入死循环）。\n\n**详细解析：** 非负整数连续除以 2，最终会变成 0；但 `0>=0` 仍为真，并且 `0/2` 还是 0，因此循环条件再也不会变为假，程序不能正常到达输出语句。严格按 C++ 规则，无限递增的有符号 x 还会最终溢出；本题要识别的直接错误是循环无法正常结束，而不是输出某个更大或更小的结果。'),
      part(20, '当输入为 `6` 时，输出为（ ）。', ['`3 3`', '`4 2`', '`4 3`', '`5 2`'], 'C', 3,
        '参考答案为 C（4 3）。\n\n**详细解析：** 循环中的 n 依次为 `6、3、1`，共三轮，所以 x 从 1 增加到 4。只有 3 和 1 为奇数，所以 y 增加两次，变为 3。逐轮状态为 `(n,x,y)：(6,2,1)→(3,3,2)→(1,4,3)`，此处 n 表示该轮除以 2 之前的值。'),
      part(21, '若输入 `n` 依次取遍 `0,1,2,…,2^31-1` 中的所有整数，则程序输出的第二个数恰好为 2 的次数为（ ）。',
        ['16', '30', '31', '32'], 'C', 3,
        '参考答案为 C（31）。\n\n**详细解析：** 每轮 `n%2` 检查当前最低二进制位，`n/=2` 则去掉这一位。因此 `y=1+二进制中1的个数`。y=2 要求只有一个 1，也就是 n 为 2 的幂。在给定范围内有 `2^0、2^1、…、2^30`，共 31 个；0 的 y 为 1，`2^31` 又超出上界。'),
    ]),
  program('reading', 2, '```cpp\n#include <algorithm>\n#include <iostream>\n#include <string>\nusing namespace std;\nint a[100007], b[100007], c[100007], carry[100007];\nstring input_str;\nint a_len, b_len;\nint main() {\n    cin >> input_str;\n    a_len = input_str.size();\n    for (int i = 0; i < a_len; i++) {\n        a[i] = input_str[a_len - i - 1] - \'0\';\n    }\n    cin >> input_str;\n    b_len = input_str.size();\n    for (int i = 0; i < b_len; i++) {\n        b[i] = input_str[b_len - i - 1] - \'0\';\n    }\n    carry[0] = 0;\n    for (int i = 0; i < max(a_len, b_len) + 1; i++) {\n        c[i] = a[i] + b[i] + carry[i];\n        if (c[i] >= 10) {\n            carry[i + 1] = 1;\n            c[i] -= 10;\n        } else {\n            carry[i + 1] = 0;\n        }\n    }\n    for (int i = max(a_len, b_len); i >= 0; i--) {\n        cout << c[i];\n    }\n    cout << endl;\n    return 0;\n}\n```\n\n本题输入的两个数均为非负整数，位数不超过 100000，可能包含前导零。',
    ['高精度运算', '数组', '字符串'], [
      part(22, '当输入为 `123 456` 时，程序输出为 `0579`。（ ）', ['正确', '错误'], 'A', 1.5,
        '参考答案为 A（正确）。\n\n**详细解析：** 数字倒序存储，依次算出个位 `3+6=9`、十位 `2+5=7`、百位 `1+4=5`，都没有进位。全局数组初始为 0，所以 `c[3]=0`。输出循环从 `max(3,3)=3` 开始，依次打印 `c[3]、c[2]、c[1]、c[0]`，得到 `0579`，不会自动去掉前导零。'),
      part(23, '假设输入的两个数均不含前导零，则程序输出的结果也一定不含有前导零。（ ）', ['正确', '错误'], 'B', 1.5,
        '参考答案为 B（错误）。\n\n**详细解析：** 输出始终多打印一个最高位，并没有检查该位是否为 0。只要两数相加没有产生额外的一位，就会打印前导零。例如 `123+456=579`，程序却打印 `0579`；输入本身都不带前导零，仍不能保证输出没有前导零。'),
      part(24, '将第 21 行改为 `c[i]=a[i]+b[i];` 后，程序输出的结果一定比原来的结果小。（ ）', ['正确', '错误'], 'B', 1.5,
        '参考答案为 B（错误）。\n\n**详细解析：** 修改后丢掉了来自低一位的进位，但并非每次加法都会有进位。输入 `123 456` 时每位相加都小于 10，原来的 `carry[i]` 全为 0，修改前后都输出 `0579`。因此“可能变小”不能推出“一定变小”。'),
      part(25, '当输入为 `12345 678` 时，输出为（ ）。', ['`012923`', '`013023`', '`13023`', '`130230`'], 'B', 3,
        '参考答案为 B（013023）。\n\n**详细解析：** 从低到高计算：`5+8=13` 留 3 进 1；`4+7+1=12` 留 2 进 1；`3+6+1=10` 留 0 进 1；`2+0+1=3`；`1+0=1`；再多算的一位为 0。输出从高到低是 `0、1、3、0、2、3`，即 `013023`。真实和为 13023，但本程序会打印前导零。'),
      part(26, '将第 22 行的 `if (c[i]>=10)` 改为 `if (c[i]>10)` 后，当输入为 `95 15` 时，输出为（ ）。',
        ['`01010`', '`110`', '`140`', '`1410`'], 'A', 3,
        '参考答案为 A（01010）。\n\n**详细解析：** 个位 `5+5=10`，错误条件 `10>10` 不成立，所以 c[0] 保留 10，且不向十位进位。十位 `9+1=10` 同样保留 10、不进位；百位为 0。输出依次打印整数 `0、10、10`，拼接成字符串 `01010`。`cout` 不会把数组元素 10 自动拆成正确的十进制进位。'),
      part(27, '假设输入的两个数均为 `n` 位正整数（不含前导零），且它们的和小于 `10^n`，则程序输出的字符串一定满足（ ）。',
        ['第一个字符一定不为 `\'0\'`', '长度一定为 `n`', '长度一定为 `n+1`，且第一个字符为 `\'0\'`', '长度可能为 `n+2`'], 'C', 3,
        '参考答案为 C。\n\n**详细解析：** 两个输入长度都为 n，输出循环从下标 n 到 0，总共输出 n+1 个数字。因为和小于 `10^n`，没有向第 n+1 位进位，所以 `c[n]=0`。原程序的进位处理保证每个 c[i] 都在 0 到 9 之间，故字符串长度恰为 n+1，首字符必为 0。'),
    ]),
  program('reading', 3, '```cpp\n#include <iostream>\nusing namespace std;\nbool check_prime(int x) {\n    if (x <= 1) return false;\n    for (int i = 2; i * i <= x; i++) {\n        if (x % i == 0) return false;\n    }\n    return true;\n}\nint n;\nvoid search_result(int x) {\n    if (!check_prime(x)) return;\n    if (x >= n) {\n        cout << x << endl;\n        return;\n    }\n    for (int i = 0; i <= 9; i++) {\n        search_result(x * 10 + i);\n    }\n}\nint main() {\n    cin >> n;\n    for (int i = 1; i <= 9; i++) search_result(i);\n    return 0;\n}\n```',
    ['深度优先搜索', '递归', '质数'], [
      part(28, '当输入为 `10` 时，程序的输出共有 10 行。（ ）', ['正确', '错误'], 'B', 1.5,
        '参考答案为 B（错误）。\n\n**详细解析：** 第一位只有 2、3、5、7 能通过质数检查。补一位后，只要仍是质数就达到 n=10 并输出，不再向下搜索。依次输出 `23、29、31、37、53、59、71、73、79`，共 9 行。注意不会输出 11、13 等数，因为前缀 1 不是质数，整条分支已被剪去。'),
      part(29, '若输入的 `n` 不大于 5，则程序的输出中一定包含 5。（ ）', ['正确', '错误'], 'A', 1.5,
        '参考答案为 A（正确）。\n\n**详细解析：** 主函数一定会调用 `search_result(5)`。5 是质数，不会在第 12 行返回；又因为 `n≤5`，条件 `5>=n` 成立，所以会打印 5。这个结论不需要知道前面的 2、3 分支输出了什么。'),
      part(30, '若输入的 `n` 大于 10，将第 17 行的 `for (int i=0; i<=9; i++)` 改为 `for (int i=1; i<=9; i+=2)` 后，程序的输出结果一定不变。（ ）',
        ['正确', '错误'], 'A', 1.5,
        '参考答案为 A（正确）。\n\n**详细解析：** 被删去的 i 为 `0、2、4、6、8`，对应新数 `10x+i` 都是偶数。能执行到追加数位这一步的 x 至少是质数 2，因此这些新数至少为 20，不可能是质数，本来也会立即返回。跳过这些没有输出的分支，不影响保留下来的奇数分支及其先后顺序。'),
      part(31, '当输入为 `24` 时，程序输出的第 3 行为（ ）。', ['23', '29', '31', '239'], 'B', 3,
        '参考答案为 B（29）。\n\n**详细解析：** 深度优先搜索先从 2 进入 23。23 虽然是质数，但还小于 24，因此继续补位：先输出 233，再输出 239。23 的分支结束后，返回到 2，继续检查 24、25、26、27、28，均非质数，随后遇到 29 并输出。因此前三行为 `233、239、29`，不能按数值大小排序来答题。'),
      part(32, '下列关于该程序输出的说法中，正确的是（ ）。',
        ['输出的数一定按照从小到大的顺序排列', '随着输入 n 的增大，输出的个数一定不会增加', '输出的数的个位数字只可能是 3 或 7', '输出的每个大于等于 10 的数，十进制下删去它的末位数字后得到的数一定是质数'], 'D', 3,
        '参考答案为 D。\n\n**详细解析：** 一个至少两位的输出数只能由父调用中的 `x*10+i` 产生。父调用必须先通过 `check_prime(x)` 才能继续递归，删去新数的末位后恰好得到这个质数 x，所以 D 成立。A 的反例是 n=24 时先输出 233 再输出 29；B 的反例是 n 从 2 增到 10，输出数由 4 个增为 9 个；C 的反例是输出 29 或 31。'),
      part(33, '当输入为 `200` 时，程序输出的行数为（ ）。', ['12', '13', '14', '15'], 'C', 3,
        '参考答案为 C（14）。\n\n**详细解析：** 一位、两位质数都小于 200，要继续补位；两位有效前缀为 `23、29、31、37、53、59、71、73、79`。分别检查追加一位后是否为质数：\n\n| 前缀 | 输出 | 个数 |\n| --- | --- | ---: |\n| 23 | 233、239 | 2 |\n| 29 | 293 | 1 |\n| 31 | 311、313、317 | 3 |\n| 37 | 373、379 | 2 |\n| 53 | 无 | 0 |\n| 59 | 593、599 | 2 |\n| 71 | 719 | 1 |\n| 73 | 733、739 | 2 |\n| 79 | 797 | 1 |\n\n总数 `2+1+3+2+0+2+1+2+1=14`。这些三位数都已达到 200，输出后立即返回，不再追加第四位。'),
    ]),
  program('completion', 1, '（进制减半）给定 `n、m`，再给定一个 `m×n` 进制下的数 A，其各个数位上的数按照从高位到低位的顺序给出，请你将其转化为 n 进制，并同样按照从高位到低位的顺序输出。\n\n输入的第一行依次为 n、m 和 A 的位数 d，接下来 d 个数 `a_d, a_(d-1), …, a_1` 从高位到低位描述各个数位上的数。\n\n数据满足 `2≤n,m≤10`，`1≤d≤18`，`0≤A<2^63`；对于所有 `1≤i≤d`，`0≤a_i<m×n`。\n\n以下程序按“逐位除以 n”的方法完成进制转换。请补全程序。\n\n```cpp\n#include <iostream>\n\nconstexpr int N = 100005;\nlong long b[N];\n\nint main() {\n    long long n, m, d;\n    std::cin >> n >> m >> d;\n    int len = 1;\n    for (int i = 0; i < d; i++) {\n        long long x;\n        std::cin >> x;\n        for (int j = len; j >= 1; j--)\n            b[j] = ①;\n        b[0] = ②;\n        len++;\n        for (int j = 0; j < len; j++)\n            if (b[j] >= n) {\n                b[j + 1] += ③;\n                b[j] = ④;\n                if (j + 1 == len) len++;\n            }\n    }\n    while (⑤) len--;\n    for (int i = len - 1; i >= 0; i--)\n        std::cout << b[i] << \' \';\n    return 0;\n}\n```',
    ['进制转换', '高精度运算', '数组'], [
      part(34, '①处应填（ ）。', ['`b[j] * n`', '`b[j] * m`', '`b[j - 1] * n`', '`b[j - 1] * m`'], 'D', 3,
        '参考答案为 D。\n\n**详细解析：** 从高位到低位读入一个新数位 x，已读部分的数值 V 应变成 `V×(m×n)+x`。数组 b 低位在前，以 n 为基数。把 `b[j-1]` 移到 b[j] 就相当于乘 n，再乘 m，合起来正好乘 m×n，所以填 `b[j-1]*m`。循环必须从高位向低位，避免旧的低位数字被覆盖。这里原进制是 m×n，不是 m。'),
      part(35, '②处应填（ ）。', ['`x * n`', '`x`', '`0`', '`m`'], 'B', 3,
        '参考答案为 B（x）。\n\n**详细解析：** 前面的右移并乘 m 已完成 `V×m×n`，接下来要加新数位 x，因此把空出的最低位设成 x。x 是原来 m×n 进制的一位，可能不小于 n，后面的进位循环会负责把它拆成合法的 n 进制数位，不能因此再乘 n 或提前置 0。'),
      part(36, '③处应填（ ）。', ['`b[j] / m`', '`b[j] % n`', '`b[j] % m`', '`b[j] / n`'], 'D', 3,
        '参考答案为 D。\n\n**详细解析：** 目标是 n 进制，每满 n 就向高一位进 1。当前 b[j] 应向 b[j+1] 贡献 `b[j]/n` 个单位。例如 n=4、b[j]=14，应进 `14/4=3`，本位留下 2。高位可能已经有值，所以使用 `+=` 合并进位；不能用原进制中的 m 来除。'),
      part(37, '④处应填（ ）。', ['`b[j] / m`', '`b[j] % n`', '`b[j] % m`', '`b[j] / n`'], 'B', 3,
        '参考答案为 B。\n\n**详细解析：** 根据除法恒等式 `b[j]=(b[j]/n)×n+b[j]%n`，商已进到高位，本位应保留余数 `b[j]%n`，其范围是 0 到 n-1。若把商留在本位，或对 m 取模，就不能保证表示原数值且每位都是合法的 n 进制数字。'),
      part(38, '⑤处应填（ ）。', ['`len > 0 && b[len - 1] == 0`', '`len > 0 && b[0] == 0`', '`len > 1 && b[len - 1] == 0`', '`len > 1 && b[0] == 0`'], 'C', 3,
        '参考答案为 C。\n\n**详细解析：** b 低位在前，最高有效位置是 `len-1`，去掉前导零应检查 `b[len-1]==0`，而不是 b[0]。同时必须保留至少一位：若输入数为 0，应输出一个 0，而不能把 len 减为 0 导致没有输出。因此条件是 `len>1 && b[len-1]==0`。'),
    ]),
  program('completion', 2, '（平衡分割）给定一个长度为 n 的字符串，其中每个字符都是一个十六进制数位。例如，字符串 `016A` 表示十进制下的四个数 `0、1、6、10`。\n\n现在请选择 k 个（k 是你选定的数）切分位置 `p_1,p_2,…,p_k`，其中 `1≤k<n` 且 `1≤p_1<p_2<…<p_k<n`，再令 `p_0=0`，`p_(k+1)=n`。\n\n对于每个 `0≤i≤k`，计算第 `p_i+1` 个数到第 `p_(i+1)` 个数的平均值，记作 b_i。你的目标是使 `b_0,b_1,…,b_k` 中最大值与最小值之差尽可能小，并输出这个最小值。\n\n其中 `2≤n≤20`。输入字符串中的字符只可能是 `0～9` 或 `A～F`。本题假定字符采用 ASCII 编码。输出答案时保留小数点后 6 位。以下程序通过递归枚举所有可能的连续分段方案，请补全程序。\n\n```cpp\n#include <algorithm>\n#include <iomanip>\n#include <iostream>\n\nusing namespace std;\n\nconstexpr int N = 25;\n\nint n, a[N];\nchar s[N];\n\ndouble ans = 1e100;\n\nint value(char c) { return ①; }\n\nvoid split(int l, int cnt, double mnb, double mxb) {\n    if (l > n) {\n        if (cnt == 0) return;\n        ans = min(ans, mxb - mnb);\n        return;\n    }\n    int sum = 0;\n    for (②) {\n        sum += a[r];\n        double nwb = ③;\n        split(④);\n    }\n}\n\nint main() {\n    cin >> n >> s + 1;\n    for (int i = 1; i <= n; ++i)\n        a[i] = value(s[i]);\n    split(⑤);\n    cout << fixed << setprecision(6) << ans;\n    return 0;\n}\n```',
    ['递归', '深度优先搜索', '枚举', '数据类型与运算'], [
      part(39, '①处应填（ ）。', ["`c - (c < '9' ? '0' : 'A' - 10)`", "`c - (c < 'A' ? '0' : 'A' - 10)`", "`c - (c < 'A' ? 'A' - 10 : '0')`", "`c - (c < 'A' ? '0' : 'A' + 10)`"], 'B', 3,
        '参考答案为 B。\n\n**详细解析：** 在允许的输入字符中，数字 `0..9` 的 ASCII 编码都小于 A，应计算 `c-\'0\'`；字母 A..F 应计算 `c-\'A\'+10`，也就是 `c-(\'A\'-10)`。B 恰好满足两种情况。A 使用 `c<\'9\'` 会漏掉字符 9，把它错分到字母分支；C 把两种偏移量交换；D 会把 A 算成 -10。'),
      part(40, '②处应填（ ）。', ['`int r = l + 1; r <= n; ++r`', '`int r = l; r < n; ++r`', '`int r = l; r <= n; r += 2`', '`int r = l; r <= n; ++r`'], 'D', 3,
        '参考答案为 D。\n\n**详细解析：** 当前段从 l 开始，右端点 r 必须枚举 `l,l+1,…,n`。从 l 开始才能包含单个元素的段，取到 n 才能完成最后一段；每次递增 1 才能不漏掉任何分段长度。每轮 `sum+=a[r]` 又恰好把区间和扩展为 `a[l]+…+a[r]`。因此选择 D。'),
      part(41, '③处应填（ ）。', ['`sum / (r - l + 1) * 1.0`', '`sum * 1.0 / (r - l) + 1`', '`sum * 1.0 / (r - l + 1)`', '`(sum - a[r]) * 1.0 / (r - l + 1)`'], 'C', 3,
        '参考答案为 C。\n\n**详细解析：** 区间 [l,r] 有 `r-l+1` 个元素，平均值应是区间和除以这个长度。先乘 1.0，保证除法是浮点除法，例如 `sum=1、长度=2` 得到 0.5。A 先做整数除法，截断后再乘 1.0 无法恢复小数；B 括号和长度都不对，且 r=l 时除以 0；D 错误地减掉了已经属于该段的最后一个元素。'),
      part(42, '④处应填（ ）。', ['`r + 1, cnt + (r < n), min(mnb, nwb), max(mxb, nwb)`', '`r + 1, cnt + (r <= n), min(mnb, nwb), max(mxb, nwb)`', '`r + 1, cnt + (r < n), max(mnb, nwb), min(mxb, nwb)`', '`r + 1, cnt + (r <= n), max(mnb, nwb), min(mxb, nwb)`'], 'A', 3,
        '参考答案为 A。\n\n**详细解析：** 下一段从 `r+1` 开始。cnt 统计的是切分位置数：当 r<n，当前段后面还有元素，才新增一个切分位置；r=n 已到末尾，不应再计一次。mnb 用 min 更新最小平均值，mxb 用 max 更新最大平均值。B 把末尾也算作切分，导致完全不切的方案也通过 `cnt==0` 检查，并错误地把答案压为 0；C、D 的最值更新方向颠倒。'),
      part(43, '⑤处应填（ ）。', ['`0, 0, 1e100, -1e100`', '`0, 0, -1e100, 1e100`', '`1, 0, -1e100, 1e100`', '`1, 0, 1e100, -1e100`'], 'D', 3,
        '参考答案为 D。\n\n**详细解析：** 字符读入 `s+1`，数组 a 的有效数据是 `a[1..n]`，所以起点为 1，不能把没有输入数据的 a[0] 也当成一段。初始还没切分，cnt=0。最小值初始设为很大的 `1e100`，最大值设为很小的 `-1e100`，第一段的平均值就能正确替换它们。配合上一空的 `cnt+(r<n)`，完全不切的方案最后 cnt 仍为 0，会被排除。'),
    ]),
];

export const csp2026YearSource = { '2026': { url: sourceUrl, status: '已导入' } };
