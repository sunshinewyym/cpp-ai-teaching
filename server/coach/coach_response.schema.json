{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Algorithm Coach Response",
  "type": "object",
  "required": [
    "stage",
    "mode",
    "focus",
    "key_hint",
    "coach_message",
    "question",
    "choices",
    "hint_level",
    "progress",
    "state_patch"
  ],
  "properties": {
    "stage": {
      "type": "string",
      "enum": ["INGEST", "EXPLORE", "MODEL", "VALIDATE", "PLAN", "COMPLETE"]
    },
    "mode": {
      "type": "string",
      "enum": ["GUIDE", "CHECKPOINT", "SUMMARY"]
    },
    "focus": {"type": "string", "minLength": 1, "maxLength": 100},
    "key_hint": {"type": "string", "minLength": 1, "maxLength": 520},
    "coach_message": {"type": "string", "minLength": 1, "maxLength": 1400},
    "question": {"type": "string", "maxLength": 180},
    "choices": {
      "type": "array",
      "minItems": 0,
      "maxItems": 3,
      "uniqueItems": true,
      "items": {
        "type": "object",
        "required": ["id", "text", "intent"],
        "properties": {
          "id": {"type": "string", "pattern": "^[A-C]$"},
          "text": {"type": "string", "minLength": 1, "maxLength": 70},
          "intent": {"type": "string", "minLength": 1, "maxLength": 80}
        },
        "additionalProperties": false
      }
    },
    "hint_level": {"type": "integer", "minimum": 0, "maximum": 5},
    "progress": {
      "type": "object",
      "required": ["goal_understood", "sample_understood", "candidate_method_found", "method_validated", "ready_for_reflection"],
      "properties": {
        "goal_understood": {"type": "boolean"},
        "sample_understood": {"type": "boolean"},
        "candidate_method_found": {"type": "boolean"},
        "method_validated": {"type": "boolean"},
        "ready_for_reflection": {"type": "boolean"}
      },
      "additionalProperties": false
    },
    "state_patch": {
      "type": "object",
      "required": ["primary_blocker", "last_strategy", "student_evidence", "revealed_insights"],
      "properties": {
        "primary_blocker": {"type": "string"},
        "last_strategy": {"type": "string"},
        "student_evidence": {"type": "array", "items": {"type": "string"}},
        "revealed_insights": {"type": "array", "items": {"type": "string"}}
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
